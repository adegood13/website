---
/**
 * Blog listing — mirrors askbobai.com/resources-blog.
 *
 * One featured card at the top (large, tag/date pill, big title) followed
 * by a 3-column grid of the remaining posts. Data source: the single
 * `blog-posts.json` content file (also used by the [slug] detail route).
 */
import Layout from "../layouts/Layout.astro";
import CTABanner from "../components/CTABanner.astro";
import posts from "../content/blog-posts.json";

const [featured, ...rest] = posts;
---

<Layout
  title="AskBobAI — Blog"
  description="Ideas, playbooks, and trends on AI, knowledge management, and making teams move faster."
>
  <!-- Header -->
  <section class="bg-white pt-28 lg:pt-36 pb-8">
    <div class="max-w-[1200px] mx-auto px-6 lg:px-10 text-center">
      <h1 class="text-5xl lg:text-7xl font-bold tracking-tight">Our Blog</h1>
      <p class="mt-4 text-ink-muted text-lg max-w-2xl mx-auto">
        Ideas, playbooks, and trends from the AskBobAI team.
      </p>
    </div>
  </section>

  <!-- Featured post -->
  <section class="bg-white pb-12">
    <div class="max-w-[1200px] mx-auto px-6 lg:px-10">
      <a
        href={`/resources-blog/${featured.slug}`}
        class="group grid md:grid-cols-2 gap-8 lg:gap-12 items-center rounded-3xl overflow-hidden"
      >
        <div class="aspect-[16/10] overflow-hidden rounded-3xl bg-blue-bg order-first md:order-last">
          <img
            src={featured.image}
            alt={featured.title}
            class="w-full h-full object-cover group-hover:scale-[1.02] transition-transform duration-500"
            loading="eager"
          />
        </div>
        <div>
          <div class="flex items-center gap-2 text-xs font-semibold mb-4">
            <span class="bg-brand-bg text-brand-dark px-3 py-1 rounded-full">{featured.category}</span>
            <span class="text-ink-muted">·</span>
            <span class="text-ink-muted">{featured.date}</span>
          </div>
          <h2 class="text-3xl lg:text-5xl font-bold tracking-tight leading-[1.1] group-hover:text-brand transition-colors">
            {featured.title}
          </h2>
          <p class="mt-5 text-ink-muted text-lg leading-relaxed">{featured.excerpt}</p>
          <p class="mt-6 inline-flex items-center gap-1 text-sm font-semibold text-brand">
            Read article
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M5 12h14M13 5l7 7-7 7"/></svg>
          </p>
        </div>
      </a>
    </div>
  </section>

  <!-- Grid of remaining posts -->
  <section class="bg-white pb-20 lg:pb-28">
    <div class="max-w-[1200px] mx-auto px-6 lg:px-10 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {rest.map(post => (
        <a
          href={`/resources-blog/${post.slug}`}
          class="group block rounded-2xl overflow-hidden border border-black/5 hover:shadow-card transition-shadow bg-white"
        >
          <div class="aspect-[16/10] overflow-hidden bg-blue-bg">
            <img
              src={post.image}
              alt={post.title}
              class="w-full h-full object-cover group-hover:scale-[1.02] transition-transform duration-500"
              loading="lazy"
            />
          </div>
          <div class="p-6">
            <div class="flex items-center gap-2 text-xs font-semibold mb-3">
              <span class="bg-brand-bg text-brand-dark px-2 py-0.5 rounded-full">{post.category}</span>
              <span class="text-ink-muted">·</span>
              <span class="text-ink-muted">{post.date}</span>
            </div>
            <h3 class="text-xl font-bold leading-tight group-hover:text-brand transition-colors">{post.title}</h3>
            <p class="mt-3 text-sm text-ink-muted leading-relaxed">{post.excerpt}</p>
          </div>
        </a>
      ))}
    </div>
  </section>

  <CTABanner heading="Get Started Today!" />
</Layout>
